<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Library grid reads the device's photos via MediaStore. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- API 34+ "select photos" partial access. -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />
    <!-- Pre-API 33 equivalent. -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:largeHeap="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HdrStacker">

        <!-- Single-activity UI shell: Library / Edit / Cloud tabs. -->
        <activity
            android:name=".studio.StudioActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.PhotoStudio">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- HDR feature (unchanged); launched from the library's selection bar. -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="HDR Stacker"
            android:theme="@style/Theme.HdrStacker" />

        <!-- Stage 1: panorama stitching. -->
        <activity
            android:name=".panorama.PanoramaActivity"
            android:exported="false"
            android:label="Panorama Stitcher"
            android:theme="@style/Theme.HdrStacker" />

    </application>

</manifest>
